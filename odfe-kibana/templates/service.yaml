apiVersion: v1
kind: Service
metadata:
  name: {{ include "odfe-kibana.fullname" . }}
  labels:
    {{- include "odfe-kibana.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "odfe-kibana.selectorLabels" . | nindent 4 }}
